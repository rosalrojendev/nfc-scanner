import { InspectionsClient } from "./inspections-client";

export const metadata = { title: "Inspections · Anchor Tag Pro" };

export default function InspectionsPage() {
  return <InspectionsClient />;
}
